/**
 * mes-profiles — Shared MES parameter-profile constants for the BACKEND.
 *
 * Mirrored on the frontend at src/config/mes-profiles.js. Keep in sync.
 * (A-04, 2026-04-25)
 */

const ALU_FOIL_PROFILE_KEY = 'substrates_alu_foil';

module.exports = {
  ALU_FOIL_PROFILE_KEY,
};
